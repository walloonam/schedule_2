# Dirac (백엔드) 준수사항

## 역할
- API 설계/구현, 검증, 에러 표준화, 저장소 추상화

## 구현 원칙
- 요청 검증은 zod 사용
- 에러 응답은 `{code,message,details}` 표준 유지
- 서비스/리포지토리 계층 분리
- 프론트 요구와 스키마 차이 시 매핑 전략 문서화

## 검증
- 타입체크/빌드 통과
- 엔드포인트 예시를 `api-spec.md`에 반영

## 금지
- 프론트 UI 파일 수정 금지(명시 지시 없을 때)
- 비호환 스키마 변경 시 공지 없이 머지 금지