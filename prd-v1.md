# 일정관리 웹앱 PRD v1

## 1. 배경 및 현재 상태
- 디자인 방향: Notion Calendar, Linear, Google Calendar의 장점을 섞은 미니멀 SaaS 스타일
- UX 원칙: 넉넉한 여백, 절제된 색상, 부드러운 카드/모달, 낮은 그림자, 직관적인 사용 흐름
- 현재 구현 상태
  - Home / Calendar / Briefing 3분 구조가 존재함
  - `Home`은 오늘 일정 요약과 빠른 이동 중심 화면임
  - `Calendar`는 검색/필터와 월간 캘린더 작업면 중심 화면임
  - `Briefing`은 우선순위, 집중 블록, 긴급 일정 요약 전용 화면임
  - 좌측 사이드바 대신 메뉴/필터 진입을 단순화한 캘린더 작업면이 존재함
  - 일정 생성/수정/삭제 모달이 존재함
  - 모바일에서는 일정 리스트 중심 UI가 존재함
  - 다크모드 토글과 로딩 skeleton이 존재함
  - PostgreSQL + SQL migration 기반 저장소가 연결되어 있음
  - Next.js `dev` 환경은 CSS 자산 응답이 불안정할 수 있어 UI 확인은 `build + start` 기준이 더 안정적임

## 2. 제품 목표
- 사용자가 오늘과 이번 주의 일정을 빠르게 확인하고, 최소 입력으로 일정을 추가/수정/삭제할 수 있게 한다.
- 일정 누락을 줄이고, 모바일과 데스크톱에서 동일한 핵심 흐름을 제공한다.
- 홈에서는 판단, 캘린더에서는 편집, 브리핑에서는 우선순위 파악에 집중하도록 화면 역할을 분리한다.

## 3. MVP 범위
### 포함
- 일정 CRUD
- 월간 캘린더 보기
- 오늘 기준 요약 화면
- 브리핑 전용 화면
- 오늘로 이동, 이전/다음 탐색
- 캘린더별 노출 제어
- 태그 필터
- 검색
- 모바일 일정 리스트
- 다크모드
- 로딩 skeleton, empty state

### 비범위
- 외부 캘린더 연동
- 로그인/권한 관리
- 팀 협업, 공유 캘린더, 멘션
- 알림/푸시/이메일 리마인더
- 반복 규칙의 고도화
- 드래그앤드롭 편집
- 통계, 리포트, 검색 히스토리
- 서버 저장소를 전제로 한 동기화/오프라인 처리

## 4. 핵심 사용자 시나리오
1. 사용자는 `Home`에서 오늘 할 일, 긴급 일정, 빠른 이동 CTA를 확인한다.
2. 사용자는 `Calendar`에서 검색/필터를 사용해 이번 달 일정을 탐색하고 새 일정을 등록한다.
3. 사용자는 일정 항목을 열어 시간, 태그, 설명을 수정한다.
4. 사용자는 `Briefing`에서 긴급/중요 일정과 집중 블록 추천을 확인한다.
5. 사용자는 모바일에서 하루 일정만 리스트로 빠르게 확인한다.

## 5. 기능 명세
### 5.1 일정 생성/수정
- 입력값: 제목, 시작일시, 종료일시, 설명, 캘린더, 태그
- 제목은 필수, 나머지는 선택 또는 기본값 허용
- 저장 시 목록과 캘린더 뷰에 즉시 반영

### 5.2 일정 삭제
- 수정 모달에서 삭제 가능
- 삭제 전에는 사용자가 의도적으로 실행해야 함

### 5.3 조회
- Home: 오늘 일정 요약과 빠른 이동 CTA 표시
- Calendar: 월간 그리드에 일정 블록 표시
- Briefing: 우선순위, 집중 블록, 후속 행동 요약 표시
- 오늘 버튼으로 현재 날짜 기준으로 즉시 복귀

### 5.4 필터
- 캘린더별 표시/비표시 전환
- 태그별 표시/비표시 전환
- 필터 조합은 AND 조건으로 동작

### 5.5 UX
- hover, focus, active 상태 제공
- 로딩 skeleton 제공
- 빈 상태에서 다음 행동을 안내
- 모바일에서는 그리드보다 리스트가 우선
- 다크모드 지원
- 수정 모달은 가능한 한 한 화면 안에서 핵심 조작을 끝내는 방향을 우선한다
- 작은 높이 뷰포트에서 모달 CTA가 잘리지 않아야 한다

## 6. 수용 기준
- 일정 생성
  - 제목과 날짜/시간을 입력하면 일정이 저장된다.
  - 제목이 비어 있으면 저장되지 않는다.
- 일정 수정
  - 기존 일정의 내용이 수정되면 즉시 화면에 반영된다.
- 일정 삭제
  - 삭제 실행 후 해당 일정이 목록과 뷰에서 제거된다.
- 월 뷰
  - 해당 월의 일정이 날짜별로 표시된다.
  - 오늘 날짜는 시각적으로 구분된다.
- Home
  - 오늘 일정과 빠른 이동 CTA가 우선 노출된다.
- Briefing
  - 긴급/중요 일정과 집중 추천이 요약된다.
- 필터
  - 캘린더 또는 태그를 끄면 해당 일정이 숨겨진다.
  - 여러 필터를 함께 적용할 수 있다.
- 검색
  - 제목/설명 기준으로 일정 탐색이 가능하다.
- 모바일
  - 작은 화면에서 일정 리스트가 우선 노출된다.
- 공통
  - 빈 화면은 사용자의 다음 행동을 안내한다.
  - 다크모드 전환이 정상 동작한다.
  - 작은 높이 뷰포트에서도 일정 수정 모달의 주요 CTA가 보여야 한다.

## 7. 우선순위
1. 일정 CRUD
2. Home / Calendar / Briefing 정보구조 정리
3. 캘린더/태그 필터 + 검색
4. 모바일 리스트
5. 다크모드
6. 반복 일정의 단순 지원

## 8. 추가 기능 제안
### 단기
- 검색
  - 기대효과: 일정 탐색 속도 향상
  - 복잡도: 낮음
- 상태값 추가(예정/완료/취소)
  - 기대효과: 할 일 중심 사용성 강화
  - 복잡도: 낮음
- Home 자연어 일정 추가
  - 기대효과: 입력 속도 향상, 초보 사용자 진입 장벽 감소
  - 복잡도: 중간
  - 로컬 LLM fallback은 후순위로 두고, 도입 시에는 `4 vCPU / 8GB` 이상 실험, `8 vCPU / 16GB` 권장으로 본다

### 중기
- 반복 일정
  - 기대효과: 정기 회의/루틴 관리 효율 개선
  - 복잡도: 중간
- 간단한 알림
  - 기대효과: 누락 방지
  - 복잡도: 중간

### 장기
- 외부 캘린더 동기화
  - 기대효과: 진입 장벽 감소, 사용 빈도 증가
  - 복잡도: 높음
- 팀 공유 캘린더
  - 기대효과: 협업 가시성 강화
  - 복잡도: 높음
- 통계/리포트
  - 기대효과: 일정 밀도와 운영 패턴 파악
  - 복잡도: 높음
