# 오늘 할 일 브리핑 UI 사양

## 목적
- 사용자가 로그인 직후 10초 안에 오늘의 핵심 일정, 위험도, 다음 행동을 파악한다.
- 기존 스타일은 유지한다.
  - 미니멀한 SaaS 톤
  - 절제된 색상
  - 둥근 카드
  - 약한 그림자
  - 명확한 정보 계층

## 화면 구성

### 1. 헤더 요약 카드
- 위치: 브리핑 화면 최상단
- 구성:
  - 제목: `오늘 할 일 브리핑`
  - 날짜: `2026-03-21`
  - 요약 수치: `예정 8건`, `긴급 2건`, `완료 3건`
  - 한 줄 요약 문장: `오전 회의가 집중되어 있고, 오후에는 처리 여유가 있다.`
- 규칙:
  - 숫자는 가장 먼저 읽히도록 크게 표시한다.
  - 요약 문장은 1줄로 제한한다.
  - 카드 높이는 콘텐츠가 늘어도 과도하게 커지지 않게 고정 감각을 유지한다.

### 2. 위험도 배지
- 위치: 헤더 요약 카드 우측 상단 또는 제목 아래
- 상태:
  - `낮음`
  - `보통`
  - `높음`
  - `긴급`
- 규칙:
  - 색만으로 구분하지 않고 텍스트를 함께 표시한다.
  - `높음`과 `긴급`은 경고 문구를 함께 노출한다.
  - 배지는 최대 1개만 보여준다.

### 3. 타임라인
- 목적: 오늘 일정의 시간 흐름과 공백을 빠르게 읽게 한다.
- 구성:
  - 시간축
  - 일정 블록
  - 공백 구간
  - 충돌 또는 지연 표시
- 규칙:
  - 기본은 `아침 / 오전 / 오후 / 저녁` 구간으로 나눠 요약한다.
  - 일정 블록은 시작 시간, 제목, 상태만 노출한다.
  - 30분 미만 일정은 축약형으로 표시한다.
  - 충돌 일정은 danger 색상보다 경고 아이콘과 테두리 강조를 우선한다.
  - 과밀한 구간은 `+3개 더 있음` 형태로 묶는다.

### 4. 추천 액션 3개
- 위치: 타임라인 아래
- 개수: 정확히 3개
- 예시:
  - `가장 먼저 처리`
  - `일정 재조정`
  - `팀에 공유`
- 규칙:
  - 액션은 우선순위 순으로 정렬한다.
  - 각 액션은 버튼 또는 카드형 CTA로 표현한다.
  - 3개를 초과하지 않는다.
  - 첫 번째 액션만 Primary 스타일을 사용하고, 나머지는 Secondary 또는 Tertiary로 처리한다.

### 5. 빈 상태
- 조건:
  - 오늘 일정이 없거나
  - 데이터 로딩 후 표시할 항목이 없을 때
- 구성:
  - 짧은 안내 문구
  - 보조 설명 1줄
  - 주요 행동 버튼 1개
- 예시 문구:
  - `오늘은 일정이 없습니다.`
  - `다음 일정이 생기면 여기서 바로 확인할 수 있습니다.`
- 규칙:
  - 빈 상태는 가볍고 조용하게 보이되, 다음 행동은 분명해야 한다.
  - CTA는 `일정 추가` 또는 `새 브리핑 보기` 중 하나만 노출한다.

## 시각 규칙
- 카드 배경은 본문 배경보다 한 단계 밝게 둔다.
- 모서리는 12px 전후로 통일한다.
- 여백은 16px, 섹션 간 간격은 24px 기준으로 맞춘다.
- 타이포 우선순위:
  - 제목
  - 수치
  - 일정명
  - 보조 설명
- 아이콘은 최소 사용하고, 의미 전달이 필요한 경우에만 붙인다.

## 다크모드 규칙
- 배경은 진한 남색 계열, 카드와 패널은 한 단계 밝은 표면으로 구분한다.
- 텍스트 대비를 강하게 유지하고, 보조 텍스트는 너무 흐리지 않게 한다.
- 위험도 배지는 채도보다 밝기 차이로 구분한다.
- 타임라인의 공백과 경계선은 더 얇고 절제되게 처리한다.
- 그림자는 약하게 유지하고, 경계선과 면 대비로 레이어를 구분한다.

## 모바일 규칙
- 단일 열 레이아웃으로 전환한다.
- 순서:
  1. 헤더 요약 카드
  2. 위험도 배지
  3. 추천 액션 3개
  4. 타임라인
- 모바일에서는 타임라인을 세로 리스트 중심으로 압축한다.
- 추천 액션은 가로 스크롤보다 세로 스택을 우선한다.
- 터치 타겟은 44px 이상으로 유지한다.
- 긴 문구는 2줄까지만 허용한다.
- 빈 상태 CTA는 화면 하단에서도 바로 누를 수 있게 배치한다.

## 상태 처리
- 로딩: 헤더와 타임라인에 스켈레톤 사용
- 성공: 위험도 배지 또는 추천 액션에 반영
- 오류: 타임라인 자체보다 상단에 간단한 재시도 메시지 노출
- 비어 있음: 안내 + 단일 CTA

## 구현 우선순위
1. 헤더 요약 카드
2. 위험도 배지
3. 타임라인
4. 추천 액션 3개
5. 빈 상태
6. 다크모드
7. 모바일 재배치
